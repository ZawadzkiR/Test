folder: "WYDZIAŁ1"

jobs:
  job1:
    enabled: true
    command: "python C:\\repo\\runner.py job1"
    trigger: "weekly"
    start_time: "07:30"
    days: ["MON", "WED", "FRI"]
    timeout_minutes: 15
    run_as_highest: true

  job2:
    enabled: true
    command: "python C:\\repo\\runner.py job2"
    trigger: "once"
    start_time: "2025-07-30T09:00"
    timeout_minutes: 5
    run_as_highest: false

  job3:
    enabled: false
    command: "python C:\\repo\\runner.py job3"
    trigger: "daily"
    start_time: "08:00"


import os
import yaml
import subprocess
import datetime

SCHEDULE_DIR = "schedules"

def run_cmd(cmd):
    return subprocess.run(cmd, capture_output=True, text=True, shell=True)

def task_exists(full_task_name):
    result = run_cmd(f'schtasks /Query /TN "{full_task_name}"')
    return result.returncode == 0

def delete_task(full_task_name):
    print(f"🗑 Usuwanie zadania: {full_task_name}")
    run_cmd(f'schtasks /Delete /TN "{full_task_name}" /F')

def compute_end_time(job):
    """Zwraca czas zakończenia zadania jako HH:MM (start + timeout_minutes)"""
    if job["trigger"] == "once":
        dt = datetime.datetime.fromisoformat(job["start_time"])
    else:
        t = job.get("start_time", "08:00")
        dt = datetime.datetime.combine(datetime.date.today(), datetime.datetime.strptime(t, "%H:%M").time())
    timeout = datetime.timedelta(minutes=job.get("timeout_minutes", 10))
    end = dt + timeout
    return end.strftime("%H:%M")

def create_or_update_task(folder, job_name, job):
    full_task_name = f"\\{folder}\\{job_name}"

    if task_exists(full_task_name):
        delete_task(full_task_name)

    cmd = job["command"]
    trigger = job.get("trigger", "daily")
    timeout = job.get("timeout_minutes", 10)
    highest = job.get("run_as_highest", False)

    schtasks_cmd = f'schtasks /Create /TN "{full_task_name}" /TR "{cmd}" /F'

    # Uprawnienia
    if highest:
        schtasks_cmd += " /RL HIGHEST"

    # Harmonogram
    if trigger == "once":
        dt = datetime.datetime.fromisoformat(job["start_time"])
        schtasks_cmd += f' /SC ONCE /ST {dt.strftime("%H:%M")} /SD {dt.strftime("%Y/%m/%d")}'
    elif trigger == "daily":
        schtasks_cmd += f' /SC DAILY /ST {job.get("start_time", "08:00")}'
    elif trigger == "weekly":
        days = ",".join(job.get("days", ["MON", "TUE", "WED", "THU", "FRI"]))
        schtasks_cmd += f' /SC WEEKLY /ST {job.get("start_time", "08:00")} /D {days}'
    elif trigger == "minute":
        interval = job.get("interval", 5)
        schtasks_cmd += f' /SC MINUTE /MO {interval}'
    elif trigger == "hourly":
        interval = job.get("interval", 1)
        schtasks_cmd += f' /SC HOURLY /MO {interval}'
    else:
        print(f"⚠️ Nieobsługiwany trigger: {trigger}")
        return

    # Czas zakończenia (timeout)
    if timeout:
        schtasks_cmd += f' /ET {compute_end_time(job)}'

    print(f"🆕 Tworzenie zadania: {full_task_name}")
    result = run_cmd(schtasks_cmd)
    if result.returncode != 0:
        print(f"❌ Błąd: {result.stderr}")
    else:
        print("✅ Zadanie utworzone")

def get_existing_tasks(folder):
    result = run_cmd(f'schtasks /Query /FO LIST /TN "\\{folder}"')
    if result.returncode != 0:
        return []
    return [
        line.split(":")[1].strip()
        for line in result.stdout.splitlines()
        if line.startswith("TaskName:")
    ]

def sync_schedule_file(path):
    with open(path, "r") as f:
        config = yaml.safe_load(f)

    folder = config["folder"]
    jobs = config.get("jobs", {})

    defined_jobs = set()

    for job_name, job in jobs.items():
        full_name = f"\\{folder}\\{job_name}"
        if job.get("enabled", True):
            create_or_update_task(folder, job_name, job)
        else:
            if task_exists(full_name):
                delete_task(full_name)
        defined_jobs.add(full_name)

    # Usuń zadania, które są w harmonogramie, ale nie w YAML
    existing_tasks = get_existing_tasks(folder)
    for task in existing_tasks:
        if task not in defined_jobs:
            delete_task(task)

def sync_all():
    for file in os.listdir(SCHEDULE_DIR):
        if file.endswith(".yaml"):
            print(f"\n🔄 Przetwarzanie {file}")
            sync_schedule_file(os.path.join(SCHEDULE_DIR, file))

if __name__ == "__main__":
    sync_all()




import sys
import importlib
import yaml
import datetime
import os

SCHEDULE_FILE = os.path.join(os.path.dirname(__file__), "schedules", "WYDZIAŁ1.yaml")  # lub dynamiczne wskazanie

def is_weekday():
    return datetime.datetime.today().weekday() < 5  # pon-pt = 0-4

def load_job_config():
    with open(SCHEDULE_FILE, "r", encoding="utf-8") as f:
        return yaml.safe_load(f).get("jobs", {})

def run_job(job_name, config):
    job = config.get(job_name)
    if not job:
        print(f"⚠️ Job '{job_name}' nie znaleziony w YAML")
        return

    if not job.get("enabled", True):
        print(f"⏭️ Pomijam '{job_name}' – disabled")
        return

    # Tylko uruchamiaj job jeśli dziś pasuje do jego harmonogramu (np. days: ["MON", "FRI"])
    trigger = job.get("trigger")
    valid_today = True

    if trigger == "weekly":
        days = job.get("days", ["MON", "TUE", "WED", "THU", "FRI", "SAT", "SUN"])
        today_str = datetime.datetime.today().strftime("%a").upper()
        if today_str not in days:
            valid_today = False

    if not valid_today:
        print(f"⏭️ Pomijam '{job_name}' – nie dzisiaj wg triggera weekly")
        return

    try:
        module = importlib.import_module(f"jobs.{job_name}")
        print(f"▶ Uruchamiam: {job_name}")
        module.main()
        print(f"✔ Zakończono: {job_name}\n")
    except Exception as e:
        print(f"❌ Błąd w {job_name}: {e}\n")

if __name__ == "__main__":
    if len(sys.argv) < 2:
        print("Użycie: python runner.py job1 [job2 ...]")
        sys.exit(1)

    job_config = load_job_config()

    for job_name in sys.argv[1:]:
        run_job(job_name, job_config)


